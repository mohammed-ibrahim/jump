[start]
seq=import-1

[import-1]
type=import
prefix=insert into teams(name, dob, slug) values 
fields=fake:name, fake:name, static:name, static:M
rows=5

[av1]
type=sql
items=select id as av from practices
